#include <iostream>
#include <vector>
#include <random>
#include <unordered_map>
#include "../conmons.h"


using namespace std;

int ant_amount = 10;
int ticks = 5;

int main(){
    parser();

    int ant_iterations = 0;
    int t = 0;

    while(t < ticks){
        ant_iterations = 0;
        while(ant_iterations < ant_amount){
            
            ant_iterations++;
        }
        
        t++;
    }

}